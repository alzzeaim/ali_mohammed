import 'package:flutter/material.dart';

class Important_links extends StatefulWidget {
  const Important_links({super.key});

  @override
  State<Important_links> createState() => _Important_linksState();
}

class _Important_linksState extends State<Important_links> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text("Important_links")),
    );
  }
}